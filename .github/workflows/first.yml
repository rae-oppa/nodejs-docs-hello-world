name: Our First Workflow
on: push
jobs:
  echo:
    runs-on: lunux-latest
    steps:
      - run: echo 'Hello, GitHub Actions!'
  pwd-ls:
    name: PWD & LS
    runs-on: lunux-latest
    steps:
      - name: Print working directory
        run: pwd
      - name: List files
        run: ls -al
